// TODO: CSV parsing utility for the student import UI.
// Responsibilities:
//   - parseCSV(file)          → parse a File object into an array of student objects
//   - validateCSVHeaders(row) → check that required columns are present
//   - sanitizeRow(row)        → trim whitespace, normalize types per field
