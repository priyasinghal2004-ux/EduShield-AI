// TODO: Authentication API functions.
// Responsibilities:
//   - POST /auth/login       → send credentials, receive JWT token
//   - POST /auth/logout      → invalidate session (if server-side session is used)
//   - Attach JWT to Axios default headers on successful login
