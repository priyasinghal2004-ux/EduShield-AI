// TODO: Prediction API functions.
// Responsibilities:
//   - POST /predictions/:studentId   → trigger prediction + SHAP explanation for a student
//   - GET  /predictions/:studentId   → fetch the most recent stored prediction for a student
//   - POST /predictions/batch        → trigger batch prediction for all students (Admin only)
