// TODO: Custom hook for consuming AuthContext.
// Responsibilities:
//   - Return user, token, login(), logout(), isAuthenticated from AuthContext
//   - Throw an error if used outside of AuthProvider
