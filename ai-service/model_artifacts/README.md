# model_artifacts/
#
# This directory holds pre-trained binary model artifacts.
# Files in this directory are GITIGNORED (.pkl files excluded).
#
# Expected files after offline training:
#   - random_forest_model.pkl   — Trained RandomForestClassifier
#   - feature_scaler.pkl        — Fitted StandardScaler
#
# To generate these files, run the offline training script
# (to be documented in docs/ARCHITECTURE.md).
